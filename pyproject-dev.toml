[build-system]
requires = [
    "scikit-build-core",
    "pxr-arch-dev==26.8.*",
    "pxr-tf-dev==26.8.*",
    "pxr-js-dev==26.8.*",
    "pxr-trace-dev==26.8.*",
    "pxr-work-dev==26.8.*",
    "pxr-boost-dev==26.8.*",
    "pxr-tbb-dev==2023.1.0.*"
]
build-backend = "scikit_build_core.build"

[project]
name = "pxr-plug-dev"
version = "26.8"
description = "C++ headers and CMake config for developing with pxr-plug"
readme = "README.md"
requires-python = ">=3.8"
license-files = ["LICENSE.txt", "NOTICE.txt"]
keywords = ["pixar", "open-usd", "dev"]
classifiers = [
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows :: Windows 10",
]
dependencies = [
    "pxr-arch-dev==26.8.*",
    "pxr-tf-dev==26.8.*",
    "pxr-js-dev==26.8.*",
    "pxr-trace-dev==26.8.*",
    "pxr-work-dev==26.8.*",
    "pxr-boost-dev==26.8.*",
    "pxr-tbb-dev==2023.1.0.*"
]

[project.urls]
Repository = "https://github.com/untwine/pxr-plug"

[tool.scikit-build]
cmake.version = ">=3.23,<4.3"
cmake.args = [
    "-DBUILD_TESTS=OFF",
    "-DBUILD_SHARED_LIBS=ON",
    "-DBUILD_PYTHON_BINDINGS=ON",
    "-DCMAKE_INSTALL_LIBDIR=pxr-plug/lib",
    "-DCMAKE_INSTALL_BINDIR=pxr-plug/lib",
    "-DCMAKE_INSTALL_INCLUDEDIR=pxr-plug/include",
    "-DCMAKE_INSTALL_DATAROOTDIR=pxr-plug",
    "-DCMAKE_INSTALL_PYTHON_LIBDIR=pxr-plug/lib/python",

    # macOS 10.13+ is required for aligned deallocation used by oneAPI TBB
    "-DCMAKE_OSX_DEPLOYMENT_TARGET=10.13"
]
sdist.exclude = ["test", ".github"]
